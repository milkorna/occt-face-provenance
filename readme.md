# MCAD_Test

Тестовое задание на C++ с использованием OpenCascade.

## Сборка

Открыть `MCAD_Test.sln` в Visual Studio 2026 и собрать проект в конфигурации `x64`.

Зависимости восстанавливаются автоматически через `vcpkg`.